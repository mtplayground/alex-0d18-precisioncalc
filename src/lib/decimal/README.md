# Decimal Arithmetic Module

This module is the single boundary for precision-safe decimal math. Calculation code should use
the exported helpers instead of JavaScript arithmetic operators when operating on user-entered
numeric values.
